# k8s 프로파일: 전용 프로브 + 컨테이너 메모리에 맞게 커넥션 풀 축소
management:
  endpoint:
    health:
      probes:
        enabled: true

spring:
  jmx:
    enabled: false
  datasource:
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
  data:
    jpa:
      repositories:
        bootstrap-mode: deferred
