# MSA 처음 접하는 개발자를 위한 Q&A (MSA Beginner Q&A)

본 문서는 e-커머스 플랫폼 (Sparta E-commerce MSA Architecture) 프로젝트를 진행하면서, 기존의 모놀리식(Monolithic) 아키텍처에 익숙한 개발자가 마이크로서비스 아키텍처(MSA)를 처음 접할 때 가질 수 있는 대표적인 궁금증 3가지와 그에 대한 답변입니다.

---

## Q1. 전체 서비스 코드를 하나의 폴더가 아닌 7개(User, Product 등)로 나누었는데, 각각 다른 데이터베이스(DB)를 사용하는 이유는 무엇인가요?

**A1.**  
가장 큰 이유는 **서비스 간의 결합도(Coupling)를 낮추고 독립성을 보장하기 위해서**입니다.
모놀리식 환경에서는 하나의 거대한 DB에 모든 테이블(User, Order, Product 등)이 모여 있어, 하나의 서비스를 수정하거나 장애가 발생하면 다른 서비스까지 그 영향을 받게 됩니다. (Single Point of Failure)

하지만 **Database per Service** 패턴을 적용하여, 예를 들어 주문(Order) 서비스는 주문 DB만 사용하고 상품(Product) 서비스는 상품 DB만 사용하게 되면:
- 주문 폭주로 인해 주문 DB에 부하가 생겨도 상품 DB나 유저 DB는 정상 작동하므로, 시스템 전체가 다운되는 일을 막을 수 있습니다.
- 특정 서비스(예: Product)만 트래픽이 많을 때, 해당 서비스와 DB만 개별적으로 스케일 아웃(Scale-out, 확장)하기가 용이해집니다.
- 만약 테이블 간 직접 결합(Join)이 필요할 경우, 직접 DB 타임에 쿼리로 묶는 대신 각 서비스가 제공하는 API 통신이나 이벤트를 통해 데이터를 조립(Aggregation)하는 방식으로 해결합니다.

---

## Q2. 클라이언트(프론트엔드)가 각 개별 서비스(포트 8080~8086)에 직접 요청하지 않고, 굳이 API Gateway(포트 8000)를 거치는 이유는 무엇인가요?

**A2.**  
API Gateway는 MSA 환경에서 **모든 클라이언트 요청을 받아내는 단일 진입점(Single Entry Point)** 역할을 합니다. 만약 클라이언트가 각 서비스의 주소와 포트를 모두 알아야 한다면:
- 백엔드 서비스의 URL이나 포트가 변경될 때마다 프론트엔드 코드도 모두 수정해야 하는 번거로움이 생깁니다.
- 공통된 보안(인증/인가) 처리를 서비스 개수만큼 7번이나 각각 구현해야 합니다.

API Gateway를 도입하면 다음과 같은 이점이 있습니다:
- **라우팅(Routing)**: 클라이언트는 오직 Gateway 주소(`http://localhost:8000`)만 알면 됩니다. 요청 경로(예: `/api/users`, `/api/orders`)에 따라 Gateway가 알맞은 내부 마이크로서비스로 요청을 자동으로 포워딩합니다.
- **공통 관심사 처리(Cross-Cutting Concerns)**: JWT 검증, 로깅, CORS 설정, Rate Limiting(요청 횟수 제한)과 같은 공통 로직을 Gateway 필터(Filter) 단에서 한 번에 처리하므로, 각 마이크로서비스는 오로지 본인들의 비즈니스 핵심 로직에만 집중할 수 있습니다.

---

## Q3. 주문 중에 상품 재고가 부족하거나 결제가 실패하면 어떻게 되나요? 모놀리식의 `@Transactional`처럼 한 번에 롤백(Rollback)할 수 없나요?

**A3.**  
MSA에서는 각 서비스가 별도의 DB를 가지고 있으므로, 여러 서비스에 걸친 작업을 전통적인 단일 DB 트랜잭션(예: RDBMS의 `@Transactional`)으로 한 번에 묶어서 커밋하거나 롤백할 수 없습니다.

이를 해결하기 위해 이 프로젝트에서는 **Saga 패턴(Saga Pattern)** 과 **Apache Kafka**를 활용하여 데이터 정합성을 맞춥니다.
- 연속된 작업 모델: `주문 생성(Order DB)` -> `재고 차감(Product DB)` -> `결제 진행(Payment DB)`
- 만약 결제 단계에서 실패가 발생한다면:
    1. 결제 서비스가 "결제 실패" 이벤트를 Kafka 메시지 브로커로 발행(Publish)합니다.
    2. 이 메시지를 구독(Consume)하고 있던 관련 서비스들(Order, Product 등)이 해당 메시지를 수신합니다.
    3. 상품 서비스는 차감했던 재고를 다시 원상 복구(+Plus) 시키고, 주문 서비스는 주문 상태를 '취소(CANCELED)'로 변경합니다.
- 이처럼 하나의 큰 트랜잭션이 실패했을 때, **이전에 성공했던 작업들을 논리적으로 취소시키는 로직을 실행하는 것**을 **보상 트랜잭션(Compensating Transaction)** 이라고 부릅니다. 이 방식을 통해 최종적으로 각 DB의 데이터 일관성을 유지(Eventual Consistency)하게 됩니다.
